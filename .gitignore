# .NET Build outputs & caches
[Bb]in/
[Oo]bj/
*.user
*.suo
*.userosscache
*.sln.docstates
*~
*.lock.json

# Visual Studio / Rider / IDE specific folders
.vs/
.idea/
*.rsuser

# Environment and Local Secrets (CRITICAL SECURITY)
.env
*.env
appsettings.Development.json       # Local dev secrets — NEVER commit

# appsettings.Testing.json and appsettings.Production.json ARE committed
# (they contain no real secrets — values injected by Azure Pipeline at runtime)
# .env.example IS committed — it is a safe template with no real values

# OS specific temporary files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store